# {{{ course }}} {{{ benchmark }}} {{{ session }}}

The complete installation process is available from [the Test-Maker project website](https://github.com/iandennismiller/test-maker/blob/master/Readme.md)

Scaffold a new project:

    workon test-maker
    cdproject
    mrbob scaffold -w -O {{{ project_path }}}

Render an exam:

    workon test-maker
    cd {{{ project_path }}}
    make
